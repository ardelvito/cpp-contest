#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
    //set the variable
    int a, b, c;
    //ask user to input
    cin >> a >> b >> c;
    //calculate the sum and display in console
    cout << (a + b + c);
    return 0;
}
